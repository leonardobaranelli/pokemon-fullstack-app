const errorHandler = require("./errorHandler");
const infoPokemonAPI = require("./infoPokemonAPI");
const infoPokemonDB = require("./infoPokemonDB");
const isIntegerID = require("./isIntegerID");
const randomImage = require("./randomImage");

module.exports = {
  errorHandler,
  infoPokemonAPI,
  infoPokemonDB,  
  isIntegerID,
  randomImage,
};